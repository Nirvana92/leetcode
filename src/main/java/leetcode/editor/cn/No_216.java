package leetcode.editor.cn;

import java.util.List;

/**
 * @author gzm
 * @date 2020/9/11 4:41 下午
 * @desc
 */
public class No_216 {
    public List<List<Integer>> combinationSum3(int k, int n) {
        //for (int startNum = 1; startNum <= 9; startNum++) {
        // 遍历第一个数是从 startNum 开始的

        //}
        int maxVal = 9;
        int[] paths = new int[k];

        return null;
    }

    /**
     * 从 startNum 开始 ~ 9 的数字中满足 k 个数, 最后满足n 的集合
     *
     * @param k
     * @param n
     * @param startNum
     */
    List<List<Integer>> process(int k, int n, int startNum, int maxVal, int[] paths, List<List<Integer>> results) {
        if (k == 0 && n == 0) {
            // 剩余 0个数, 剩余总
        }

        return null;
    }
}
